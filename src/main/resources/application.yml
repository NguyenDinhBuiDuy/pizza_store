# Server Configuration
server:
  port: 8086

#Data Configuration
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pizza_store
    username: postgres
    password: admin
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update